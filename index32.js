const hight = 10;
const base = 4;
const perimeter = ( base + hight )*2;
const area = (base * hight);
let template =`in a rectangular with height = ${hight} and base = ${base} then area is ${area} and perimeter is ${perimeter}`;
console.log(template);